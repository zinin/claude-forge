# Continuation prompt: build-runner Gradle hardening (после исполнения плана, до смоук-тестов)

Скопируйте всё, что ниже разделителя, в новую сессию Claude Code, запущенную в `/opt/github/zinin/claude-forge`.

Этот файл ЗАМЕНЯЕТ предыдущий continuation prompt (тот был написан до исполнения плана; теперь все 4 задачи выполнены и отревьюены).

---

## TASK

Заверши хвост плана «Build-runner Gradle Hardening» (ветка `build-runner-hardening`). Файловая работа плана ВЫПОЛНЕНА и отревьюена; финальный вердикт whole-branch ревью: **Ready to merge — Yes** с гейтом. Остались: смоук-тесты (гейт мержа), опциональная полировка миноров, finishing (удаление `docs/superpowers/`, PR).

## CRITICAL: DO NOT START WORKING

**СТОП. Прочитай внимательно.**

После загрузки контекста ты ОБЯЗАН:
1. Прочитать документы и понять контекст.
2. Кратко изложить своё понимание.
3. **ЖДАТЬ явной команды пользователя перед ЛЮБЫМИ действиями.**

**НЕЛЬЗЯ:**
- запускать смоук-тесты, менять код/файлы, коммитить;
- выполнять команды (кроме чтения документов);
- самому решать, что делать дальше.

**Пользователь сам скажет, что делать.** До этого — только чтение и резюме.

## DOCUMENTS

- Design: `docs/superpowers/specs/2026-07-17-build-runner-gradle-hardening-design.md`
- Plan (обрезан до статусов; Task 4 Step 3 — единственный открытый шаг): `docs/superpowers/plans/2026-07-17-build-runner-gradle-hardening.md`
- Леджер исполнения (прогресс, все минорки с координатами, вердикты ревью): `.superpowers/sdd/progress.md` — git-ignored scratch, лежит на диске; читать ОБЯЗАТЕЛЬНО.
- Лог решений ревью плана (по необходимости): `docs/superpowers/specs/2026-07-17-build-runner-gradle-hardening-review-iter-1.md`

## PROGRESS

**Выполнено (закоммичено; каждая задача прошла spec+quality ревью, фиксы — с ре-ревью):**
- [x] Task 1: `agents/build-runner.md` — `655f5ef`
- [x] Task 2: `skills/build/SKILL.md` — `0cc7627`
- [x] Task 3: `README.md` — `2d1dc69` + ревью-фикс `ae1a6d1` (добавлен `ps` в Recovery permissions — одобрено пользователем)
- [x] Task 4 Steps 1/2/4: CHANGELOG — `f64f854` + ревью-фикс `3c1ea76` (формулировка таймаута — одобрено пользователем)
- [x] Финальное whole-branch ревью (d972c69..3c1ea76): 0 Critical, 0 Important; критерии приёмки 1–5 проверены попунктно; Ready to merge — Yes.

**Осталось:**
- [ ] Task 4 Step 3: смоук-тесты вручную из ветки (`claude --plugin-dir /opt/github/zinin/claude-forge`, реальный Gradle-проект, 3 сценария из плана) — ГЕЙТ МЕРЖА. Проект для тестов НЕ выбран — согласовать с пользователем, самому не изобретать.
- [ ] (опционально, только по явной команде пользователя) Полировка миноров одним коммитом: F1–F4 + M7 и пр. из леджера (все оттриажены ship-as-is, поведение не меняют).
- [ ] Finishing (superpowers:finishing-a-development-branch): `git rm -r docs/superpowers/` отдельным коммитом (правило пользователя), затем PR в master.

## SESSION CONTEXT

Решения, зафиксированные в ходе исполнения (НЕ пересматривать):

- **Трейлер коммитов**: у локальной CLI-сессии web-URL нет — по решению пользователя в трейлер пишется локальный session id ИСПОЛНЯЮЩЕЙ сессии: `Claude-Session: <твой локальный session id>` (свой id видно, например, в пути scratchpad-директории из системного промпта). Сессия-исполнитель плана использовала `9dced053-ad2b-440b-bbd9-1b7f39c76336` — НЕ переиспользуй чужой id.
- **Два одобренных пользователем отступления от буквы плана** (не «откатывать к плану»): README Recovery permissions включает `ps`/`Bash(ps:*)` (`ae1a6d1`); буллет CHANGELOG — «explicit 30-minute Bash timeout ask on every build command (dependency installs included; harness-clamped to the host ceiling)» (`3c1ea76`).
- Решения брейншторма/ревью неизменны: `model: sonnet`; у агента НЕТ `kill`; `timeout: 1800000` (харнес молча клампит) — НЕ менять на 600000; recovery хирургический (identity-проверка `ps -fp`, чужие процессы — только через пользователя); метки: полная строка у агента, короткий префикс в скилле — сознательно, не рассинхрон.
- **Минорки (11 шт.)**: M1–M7 из пофазовых ревью + F1–F4 из финального — ВСЕ ship-as-is, координаты и формулировки в `.superpowers/sdd/progress.md`. Чинить только по явной команде пользователя.
- **Смоук-тест, сценарий 3 — дополнительные проверки от финального ревью**: (a) префикс-матч метки `run daemon recovery procedure first (see build skill)` против короткой в скилле; (b) проходит ли пайп `jps -lv | grep -E '…'` агента через allowlist `Bash(jps:*)` (семантика разрешений для составных команд различается между версиями Claude Code). Если пайп режется — правка инструкции агента на чистый `jps -lv` + фильтрация Grep-инструментом (это изменение `agents/build-runner.md` — согласовать с пользователем).
- Git-гигиена прежняя: `git add` только явные пути (никаких `docs/`, `-A`, `-am`); `docs/gradle-build-runner-hardening-report.md` untracked — НЕ коммитить; кириллицы в продуктовых файлах (`agents/`, `skills/`, README, CHANGELOG) быть не должно.
- SDD-исполнение плана ЗАВЕРШЕНО — оставшаяся работа интерактивная; заново запускать /claude-mesh:do-plan не нужно (если пользователь не скажет иначе).
- `.superpowers/sdd/` — рабочий scratch исполнения (брифы задач, отчёты имплементеров, дифф-пакеты ревью, progress.md); git его не отслеживает.

## PLAN QUALITY WARNING

План и исполнение прошли многослойное ревью, но смоук-тесты впервые сталкивают инструкции с реальным поведением харнеса и Gradle — возможны расхождения.

**Если что-то идёт не так:**
1. ОСТАНОВИСЬ.
2. Чётко опиши проблему.
3. Объясни, почему поведение расходится с ожидаемым.
4. Спроси пользователя, как действовать.

Не обходи проблемы молча и не отклоняйся от плана существенно без одобрения пользователя.

## INSTRUCTIONS

1. Прочитай документы из списка выше (леджер `.superpowers/sdd/progress.md` — обязательно).
2. Пойми текущий прогресс и контекст.
3. Кратко изложи понимание.
4. **СТОП и ЖДИ** — никаких действий.
5. Спроси: «С чего начинаем?»
