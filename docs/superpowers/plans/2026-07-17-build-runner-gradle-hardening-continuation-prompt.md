# Continuation prompt: build-runner Gradle hardening (после mesh-review, до смоук-тестов)

Скопируйте всё, что ниже разделителя, в новую сессию Claude Code, запущенную в `/opt/github/zinin/claude-forge`.

Этот файл ЗАМЕНЯЕТ предыдущий continuation prompt (тот был написан до внешнего mesh-review; теперь оно проведено, применены два коммита правок; HEAD ветки — `1f291ae`).

---

## TASK

Заверши хвост плана «Build-runner Gradle Hardening» (ветка `build-runner-hardening`). Файловая работа ВЫПОЛНЕНА и многократно отревьюена: SDD (spec+quality на задачу) → whole-branch ревью (Ready to merge — Yes) → внешнее mesh-review из 7 ревьюеров, давшее ещё две правки (`07f2a20`, `1f291ae`). Остались: смоук-тесты (гейт мержа), опциональные решения (F1 + косметика), finishing (удаление `docs/superpowers/`, PR).

## CRITICAL: DO NOT START WORKING

**СТОП. Прочитай внимательно.**

После загрузки контекста ты ОБЯЗАН:
1. Прочитать документы и понять контекст.
2. Кратко изложить своё понимание.
3. **ЖДАТЬ явной команды пользователя перед ЛЮБЫМИ действиями.**

**НЕЛЬЗЯ:**
- запускать смоук-тесты, менять код/файлы, коммитить;
- выполнять команды (кроме чтения документов);
- самому решать, что делать дальше.

**Пользователь сам скажет, что делать.** До этого — только чтение и резюме.

## DOCUMENTS

- Design: `docs/superpowers/specs/2026-07-17-build-runner-gradle-hardening-design.md`
- Plan (обрезан до статусов; Task 4 Step 3 — единственный открытый шаг; статус-строка отражает mesh-review): `docs/superpowers/plans/2026-07-17-build-runner-gradle-hardening.md`
- Леджер исполнения (прогресс, все минорки с координатами, вердикты ревью, СЕКЦИЯ mesh-review с решениями по спорным): `.superpowers/sdd/progress.md` — git-ignored scratch, лежит на диске; читать ОБЯЗАТЕЛЬНО.
- Лог решений ревью плана (по необходимости): `docs/superpowers/specs/2026-07-17-build-runner-gradle-hardening-review-iter-1.md`

## PROGRESS

**Выполнено (закоммичено):**
- [x] Task 1: `agents/build-runner.md` — `655f5ef`
- [x] Task 2: `skills/build/SKILL.md` — `0cc7627`
- [x] Task 3: `README.md` — `2d1dc69` + ревью-фикс `ae1a6d1`
- [x] Task 4 Steps 1/2/4: CHANGELOG — `f64f854` + ревью-фикс `3c1ea76`
- [x] Финальное whole-branch ревью (d972c69..3c1ea76): 0 Critical, 0 Important, Ready to merge — Yes.
- [x] Внешнее mesh-review (7 ревьюеров, 6/6 врапперов REAL): `07f2a20` (авто-фиксы) + `1f291ae` (5 спорных, все одобрены). Подробности — в SESSION CONTEXT и леджере.

**Осталось:**
- [ ] Task 4 Step 3: смоук-тесты вручную из ветки (`claude --plugin-dir /opt/github/zinin/claude-forge`, реальный Gradle-проект) — ГЕЙТ МЕРЖА. Сценарий 3(b) можно снять (пайпы убраны в 5/5), сценарий 2 расширен (см. ниже). Проект для тестов НЕ выбран — согласовать с пользователем.
- [ ] (опционально, по явной команде) F1: дописать `sleep`/`./gradlew --stop` в README recovery permissions — открытое решение пользователя (см. ниже).
- [ ] (опционально) косметические миноры (все ship-as-is).
- [ ] Finishing (superpowers:finishing-a-development-branch): `git rm -r docs/superpowers/` отдельным коммитом (правило пользователя), затем PR в master.

## SESSION CONTEXT

**Mesh-review (2026-07-17) — два коммита правок поверх плана. НЕ пересматривать одобренные решения.**

`07f2a20` — авто-фиксы (бесспорные, single-fix correctness):
- Maven `-B` поднят в Execution Rules rule 4 (симметрия с `--console=plain`, дизайн §24; deepseek I2).
- `ps -o pid,stat,args -p <X>` вместо `ps -fp <X>` в агенте + скилле (recovery шаг 3) + README-runbook; убрано ложное «args shows its project», зомби-STAT виден (codex I2, minimax, review-claude).
- Разведены «dead owner» (`ps` → no such process → safe to retry) и «`ps` недоступен» (→ BUILD FAILED).
- registry-lock: приоритет owner-триажа, если в сообщении есть Owner PID.

`1f291ae` — решения по 5 спорным (ВСЕ одобрены пользователем, НЕ откатывать и НЕ переоткрывать):
- **1/5 (A):** Daemon Recovery шаг 2 — честная формулировка `--stop`: останавливает ВСЕ демоны этой версии Gradle на хосте (чужие проекты/IDE включительно), graceful, приемлемо при уже удерживаемом глобальном локе; escape-hatch на shared-хостах. Поведение НЕ изменилось (чинилась вводящая в заблуждение фраза «project's version only»; codex Critical).
- **2/5 (A):** протокол фоновых сборок — stack-aware маркеры завершения (Gradle / Maven / marker-less), ОБЯЗАТЕЛЬНОЕ чтение хвоста перед объявлением orphan, ветвление бюджета: Gradle → orphaned INFRASTRUCTURE FAILURE + recovery; прочие стеки → BUILD FAILED (согласовано с agent:205). Плюс «Poll instead of re-running» (было «re-reading»).
- **3/5 (A):** foreground `sleep` — БЕЗ правки. Эмпирически проверено в этой сессии: `sleep 3`, `sleep 30`, составная `sleep 15 && echo` — все ИСПОЛНЯЮТСЯ в главном сеансе; doc/system-prompt claim «sleep is blocked» (GLM I1, GitHub #65383) НЕ воспроизвёлся. Остаточный риск субагента/sandbox закрывает смоук-сценарий 2.
- **4/5 (A):** контракт меток на agent:472 — задокументировано, что скилл матчит `run daemon recovery procedure first` по ПРЕФИКСУ, а `safe to retry` целиком; нераспознанная метка деградирует в BUILD FAILED (fail-safe). Машинные коды остаются отложены по дизайну «Вне скоупа» (тезис qwen I2 про коды отклонён как re-litigation).
- **5/5 (B):** диагностика агента — `jps -lv` / `jstack <X>` ГОЛЫМИ (без `| grep`/`| head` — их нет в allowlist агента), фильтрация в контексте. Убрана недокументированная permission-неопределённость пайпа (deepseek I3). Спайк тут недоступен: у установленной копии плагина allowlist доветочный.

**Эмпирические факты этой сессии (не перепроверять без нужды):** foreground `sleep` в главном сеансе работает (см. 3/5).

**Смоук-тесты после mesh-review:** сценарий 3(b) (пайп `jps` через allowlist) — СНЯТ (пайпы убраны). Сценарий 2 (сборка >10 мин → фон → поллинг `sleep 30` + Grep, без перезапуска) теперь ЗАОДНО определяющая проверка «не блокируется ли `sleep` в субагенте». Три сценария из плана: (1) >2 мин форграунд с `timeout: 1800000` + `--console=plain`; (2) фон + поллинг; (3) занятый lock → INFRASTRUCTURE FAILURE с верным Owner PID + ровно один ретрай (+ проверка префикс-матча метки 3(a)).

**ОТКРЫТОЕ решение пользователя — F1:** README recovery permissions НЕ содержат `sleep` и `./gradlew --stop`, хотя процедура их запускает в главном сеансе (5 из 7 ревьюеров подняли). Пользователь РАНЕЕ (`ae1a6d1`) сознательно оставил минимальный scope (добавил только `ps`). Оставлено ship-as-is до явной команды пользователя дописать эти два (одна строка).

**Неизменные решения брейншторма/ревью:** `model: sonnet`; у агента НЕТ `kill`; `timeout: 1800000` (харнес молча клампит — НЕ менять на 600000); recovery хирургический (identity-проверка, чужие процессы — только через пользователя); метки дословно у агента, короткий префикс в скилле — сознательно.

**Трейлер коммитов:** каждый коммит заканчивается `Claude-Session: <локальный session id ИСПОЛНЯЮЩЕЙ сессии>` (у локальной CLI web-URL нет — по решению пользователя пишется её локальный id; свой id виден в пути scratchpad-директории). Эта сессия использовала `949fc472-c754-4c59-a9c4-fb329f7ccef9`; сессия-исполнитель плана — `9dced053-...`. НОВАЯ сессия ОБЯЗАНА использовать СВОЙ id, не переиспользовать чужие.

**Git-гигиена:** `git add` только явные пути (никаких `docs/`, `-A`, `-am`); `docs/gradle-build-runner-hardening-report.md` untracked — НЕ коммитить; кириллицы в продуктовых файлах (`agents/`, `skills/`, README, CHANGELOG) быть не должно. `.superpowers/sdd/` — git-ignored scratch.

**Ship-as-is миноры (не чинить без команды):** широта `Bash(kill:*)` (README уже «or confirm the prompts»); «Owner PID на следующей строке» (агент извлекает PID семантически); фолбэк `--stop` для проектов без wrapper (skip сознателен); длинный буллет CHANGELOG; оговорка README:44 про 30 мин; формулировка бюджета «4 запуска».

## PLAN QUALITY WARNING

План и исполнение прошли многослойное ревью (SDD + whole-branch + внешнее mesh-review), но смоук-тесты впервые сталкивают инструкции с реальным поведением харнеса и Gradle — возможны расхождения (особенно: работает ли `sleep`-поллинг и доставка completion-уведомлений В СУБАГЕНТЕ; корректность формата move-to-background; string-match меток).

**Если что-то идёт не так:**
1. ОСТАНОВИСЬ.
2. Чётко опиши проблему и почему поведение расходится с ожидаемым.
3. Спроси пользователя, как действовать.

Не обходи проблемы молча и не отклоняйся от плана существенно без одобрения пользователя.

## INSTRUCTIONS

1. Прочитай документы из списка выше (леджер `.superpowers/sdd/progress.md` — обязательно, включая секцию mesh-review).
2. Пойми текущий прогресс и контекст.
3. Кратко изложи понимание.
4. **СТОП и ЖДИ** — никаких действий.
5. Спроси: «С чего начинаем?»
